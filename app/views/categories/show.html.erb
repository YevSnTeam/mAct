<p>
	<b><%= @category.name %> </b>
</p>

<table>
<% @category.todos.each do |todo| %>
	<tr>
		<td><%= todo.name %></td>
	    <td><%= link_to 'Show', todo %></td>
	    <td>Anzahl gewählt</td>
	    <td>Anzahl erledigt</td>
	</tr>
<% end %>
</table>